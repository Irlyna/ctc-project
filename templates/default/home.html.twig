{% extends 'default/layout.html.twig' %}

{% block nav %}
    <div class="content">
        <nav class="ordreAlpha">
            <ul>
                <li><a href="{{ path('homepage')}}" >Tout</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'A' }) }}>A</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'B' }) }}>B</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'C' }) }}>C</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'D' }) }}>D</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'E' }) }}>E</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'F' }) }}>F</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'G' }) }}>G</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'H' }) }}>H</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'I' }) }}>I</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'J' }) }}>J</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'K' }) }}>K</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'L' }) }}>L</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'M' }) }}>M</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'N' }) }}>N</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'O' }) }}>O</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'P' }) }}>P</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'Q' }) }}>Q</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'R' }) }}>R</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'S' }) }}>S</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'T' }) }}>T</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'U' }) }}>U</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'V' }) }}>V</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'W' }) }}>W</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'X' }) }}>X</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'Y' }) }}>Y</a></li>
                <li><a href={{ path('recipe.by.letter', {'letter' : 'Z' }) }}>Z</a></li>
            </ul>
        </nav>
    </div>
{% endblock %}

{% block content %}
    <div class="display">
        {% if not recipes %}
            <p class="bg message">Désolée, aucunes recettes enregistrées à afficher pour le moment</p>
        {% endif %}



        {% for recipe in recipes %}
            <div class="bg display-recipe">
                <h2>{{ recipe.name }}</h2>
                <div class="read-recipe">
                    <ul>
                        {% for ingredient in recipe.ingredients%}
                            <li>{{ ingredient.name }}</li>
                        {% endfor %}
                    </ul>
                    <p>{{ recipe.content }}</p>
                    {% for category in recipe.recipeCategories %}
                        <span class="category">{{ category.name }}</span>
                    {% endfor %}
                    <p>Recette de {{ recipe.user.name }}</p>
                </div>
            </div>
        {% endfor %}
    </div>

{% endblock %}